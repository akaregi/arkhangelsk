# arkhangelsk

Codename “Arkhangelsk” for usage of Discord bot.

## How to use

1. Make `.env` then add `TOKEN`, `CLIENT_ID`, and `GUILD_ID`.
2. Run:
```bash
$ yarn
$ yarn initialize
$ yarn start
```
